# BusinessPoints.Scala

I've used PlayFramework for a little webserver.

To run the server:
 - Install JDK. Preferably JDK 1.8
 - Install SBT. For mac: `brew install sbt`
 - Run the project. `sbt run`